org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
android.builtInKotlin=false
android.newDsl=false
# AGP's minor-SDK-version compileSdk DSL (needed for mozilla-components 154.0's
# compileSdk 37.1 requirement) is only wired up under AGP's new DSL, which Flutter's
# Gradle plugin cannot yet apply against (flutter/flutter#180137). Until that lands,
# disable the AAR metadata compileSdk check itself rather than the whole new DSL.
android.experimental.disableCompileSdkChecks=true
